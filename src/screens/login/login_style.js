import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  not_registered_text: {
    textAlign: 'center',
    bottom: 0,
    position: 'absolute',
    left: 0,
    right: 0,
  },
  input_style: {
    borderColor: '#555555',
    borderWidth: 1,
    padding: 5,
  },
  input_view: {
    paddingBottom: 15,
  },
});

export default styles;
